Metadata-Version: 2.4
Name: storagevoice-ai
Version: 0.1.0
Summary: Multi-tenant AI voice automation for self-storage operators
Requires-Python: >=3.11
Description-Content-Type: text/markdown
Requires-Dist: fastapi>=0.116.0
Requires-Dist: uvicorn[standard]>=0.35.0
Requires-Dist: httpx>=0.28.0
Requires-Dist: pydantic>=2.11.0
Requires-Dist: pydantic-settings>=2.10.0
Requires-Dist: python-dotenv>=1.1.0
Requires-Dist: sqlalchemy>=2.0.40
Requires-Dist: alembic>=1.16.0
Requires-Dist: psycopg[binary]>=3.2.0
Requires-Dist: cryptography>=45.0.0
Provides-Extra: dev
Requires-Dist: pytest>=8.4.0; extra == "dev"
Requires-Dist: ruff>=0.12.0; extra == "dev"

# StorageVoice AI

Production-oriented, multi-tenant backend scaffold for a voice + automation platform for self-storage operators.

## Design goals

- Start with UnitTrac support immediately.
- Future-proof for any storage software provider with an API.
- Keep provider-specific logic isolated behind a stable interface.
- Support secure tenant onboarding with encrypted API credential storage.

## Current scaffold

- FastAPI API service for tenant onboarding/configuration
- Provider adapter architecture:
  - `StorageProvider` interface (provider-agnostic contract)
  - `UnitTracProvider` implementation
  - registry/factory for dynamic provider selection
- Encrypted credential storage service
- Initial endpoints:
  - list supported providers
  - create tenant and provider config
  - validate provider credentials

## Quick start

1. Create env file:
   - `cp .env.example .env`
2. Install:
   - `pip install -e .`
3. Run API:
   - `uvicorn apps.api_service.src.main:app --reload --host 0.0.0.0 --port 8000`

## Database migrations (PostgreSQL)

- Configure `DATABASE_URL` in `.env`
- Run migrations:
  - `alembic upgrade head`
- Current core tables:
  - `tenants`
  - `tenant_provider_configs`
  - `operator_contacts`
  - `audit_logs`

## Cloud deployment (recommended)

Typical layout:

- **API**: container running Uvicorn (this repo) behind HTTPS (ALB / Cloud Run / App Service / Fly.io).
- **Database**: managed PostgreSQL (RDS, Cloud SQL, Azure Database) with backups + least-privilege credentials.
- **Secrets**: store `SECRETS_ENCRYPTION_KEY` and DB credentials in a secrets manager (AWS Secrets Manager, GCP Secret Manager, Azure Key Vault), injected as env vars at deploy time.
- **Networking**: restrict Postgres to the API subnet / private link; public ingress only to the load balancer.

Build and run the API image:

```bash
docker build -f infra/docker/Dockerfile -t storagevoice-api .
docker run --rm -p 8000:8000 \
  -e DATABASE_URL="postgresql+psycopg://user:pass@host:5432/dbname" \
  -e SECRETS_ENCRYPTION_KEY="your-fernet-key" \
  storagevoice-api
```

Run migrations before or as a release step: `alembic upgrade head` (same image, override command).

## UnitTrac integration

- OpenAPI: `https://www.unittrac.com/openapi/v1.json`
- Onboarding uses `GET /api/v1/Businesses/details` to validate API keys (Bearer token).
- Availability uses `GET /api/v1/Facilities/{id}` and filters units that are active, rentable, and not leased.
- Caller phone verification may require additional UnitTrac customer endpoints when exposed for your key; the adapter returns `unit_found` vs full `verified` accordingly.

## Why provider adapters?

New management software support should only require:

1. Add a new provider package implementing `StorageProvider`.
2. Register it in `providers/registry/factory.py`.
3. Optionally add provider-specific validation schema.

No core routing or business logic rewrite needed.

## GitHub: CI and syncing

- **CI**: Pushes and pull requests to `main` run [GitHub Actions](.github/workflows/ci.yml) (install, Ruff, pytest).
- **Dependency updates**: [Dependabot](.github/dependabot.yml) opens weekly pip PRs and monthly Actions updates.
- **Push changes in one step** (after you commit locally, or it will commit for you):

```bash
./scripts/sync-push.sh "your commit message"
```

Cursor/VS Code does not auto-push to GitHub; use the script above or **Source Control → Sync** / `git push` after saving.
